# 🖥️ Red Team Ops Terminal

> **Red Team Ops Terminal** — это автономное (Single Page Application) веб-приложение для агрегации, поиска и визуализации векторов атак (TTPs), команд LOLBAS и сниппетов для Red Teaming.

Приложение работает **полностью на стороне клиента (Client-Side)**, не требует бэкенда или установки зависимостей. Просто откройте HTML-файл в браузере.

---
<img width="1916" height="862" alt="image" src="https://github.com/user-attachments/assets/036f6043-0ebe-4a55-8bb8-df484455f360" />

---

## ✨ Возможности

*   **🛠 Полная автономность:** Единый HTML-файл. Работает оффлайн без интернета и серверов.
*   **📂 Drag & Drop загрузка:** Поддержка загрузки нескольких JSON-файлов базы знаний (или папки `enriched_json`) простым перетаскиванием.
*   **🔍 Умный поиск и фильтрация:**
    *   Фильтрация по фазам Kill Chain (Recon, Initial Access, Persistence и т.д.).
    *   Мгновенный поиск по названиям, командам, описаниям и MITRE ID.
    *   Тегирование и фильтрация по ключевым словам.
*   **🛡 OpSec Аналитика (Эвристика):**
    *   Автоматическая оценка "шумности" вектора (High Noise, Moderate, Safe).
    *   Подсветка опасных действий (запись на диск, создание служб, известные хакерские утилиты).
*   **🔗 Интеграция с MITRE ATT&CK:** Кликабельные, анимированные плашки с автоматическим переходом на страницу техники MITRE.
*   **🎨 Cyberpunk UI:** Темная тема, цветовая кодировка фаз атаки, адаптивный дизайн.

---

## 🚀 Быстрый старт

### Локальный запуск
1.  Скачайте репозиторий.
2.  Откройте файл `index.html` в любом современном браузере (Chrome, Firefox, Edge).
3.  Нажмите кнопку **LOAD DB** в правом верхнем углу.
4.  Выберите все файлы `.json` из папки `enriched_json`.
    *   *Альтернатива:* Просто перетащите JSON-файлы в окно браузера.

---

## 🗂 Структура данных (JSON)

Проект ожидает JSON файлы в папке `enriched_json`. Скрипт автоматически нормализует данные, но рекомендуемая структура выглядит так:

```json
[
  {
    "Name": "Tool Name",
    "Description": "Описание вектора атаки.",
    "Author": "Author Name",
    "Commands": [
      {
        "Command": "powershell.exe -ep bypass ...",
        "Description": "Детальное описание конкретной команды",
        "MitreID": "T1059.001",
        "Privileges": "User",
        "kill_chain_phase": "EXECUTION", 
        "Tags": ["PowerShell", "Remote"],
        "Category": "Execute"
      }
    ]
  }
]
```

### Поля
*   **kill_chain_phase**: Определяет вкладку на интерфейсе (`RECON`, `ACCESS`, `PRIVESC`, `CREDS`, `LATERAL`, `PERSIST`, `EVASION`).
*   **MitreID**: Создает ссылку на официальную документацию MITRE ATT&CK.
*   **OpSec**: Рассчитывается динамически на основе содержимого команды (нет необходимости указывать вручную в JSON).

---

## 🧠 Логика OpSec (Шумность)

Терминал анализирует команды «на лету» и присваивает уровень риска на основе эвристики:

| Уровень | Описание | Критерии |
| :--- | :--- | :--- |
| 🔴 **HIGH NOISE** | Высокий риск обнаружения | Запись на диск, создание служб/задач, изменение реестра, добавление пользователей, использование известных бинарников (Mimikatz, Rubeus, Psexec). |
| 🟠 **MODERATE** | Средний риск | Активное сетевое взаимодействие, использование PowerShell, WMI, WinRM. |
| 🟢 **OPSEC SAFE** | Низкий риск | Пассивный сбор данных, работа в памяти, чтение LDAP, работа с тикетами Kerberos. |
| ⚪ **N/A** | Неизвестно | Недостаточно данных для анализа. |

---

## ⚠️ Дисклеймер

> Этот инструмент и предоставленные данные предназначены **исключительно для образовательных целей и легального тестирования на проникновение (Red Teaming)** с разрешения владельца систем. 
>
> Автор не несет ответственности за неправомерное использование предоставленной информации.
```
